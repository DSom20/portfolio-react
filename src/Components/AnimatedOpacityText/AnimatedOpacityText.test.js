import React from 'react';
import { render } from '@testing-library/react';
import AnimatedOpacityText from '.';

test('renders AnimatedOpacityText without crashing', () => {
  render(<AnimatedOpacityText />);
});