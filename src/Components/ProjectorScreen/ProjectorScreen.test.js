import React from 'react';
import { render } from '@testing-library/react';
import ProjectorScreen from '.';

test('renders ProjectorScreen without crashing', () => {
  render(<ProjectorScreen />);
});